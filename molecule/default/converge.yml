---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include ansible-roles-nginx-proxy"
      include_role:
        name: "ansible-roles-nginx-proxy"
      vars:
        graylog_fqdn: "graylog.YourDomain.pl"
        bind_ip: "127.0.0.1"
